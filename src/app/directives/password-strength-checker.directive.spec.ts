import { PasswordStrengthCheckerDirective } from './password-strength-checker.directive';

describe('PasswordStrengthCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordStrengthCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
